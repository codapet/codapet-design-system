import { Toaster } from '@codapet/design-system'

// NOTE: Toaster is the toast host region. It renders nothing visible until
// toast() is called, so this preview is intentionally empty by design.
export const Region = () => <Toaster position="bottom-right" />
